# Computer vision + ML
mediapipe==0.10.14
opencv-python>=4.9.0
deepface>=0.0.89
numpy>=1.24.0,<2.0
Pillow>=10.0.0

# Custom expression classifier (PyTorch MLP on 468 landmarks)
torch>=2.2.0
torchvision>=0.17.0

# Local LLM inference — no API key needed
# Install Ollama first: https://ollama.com/download
# Then run: ollama pull llama3.2
ollama>=0.3.0

# Local vector database — no API key needed
chromadb>=0.5.0

# Clustering
scikit-learn>=1.4.0

# Object detection (YOLOv8 — model auto-downloads on first run)
ultralytics>=8.2.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
jinja2>=3.1.3

# Training utilities
tqdm>=4.66.0

# DeepFace backend
tf-keras>=2.16.0

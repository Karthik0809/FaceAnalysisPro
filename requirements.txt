# Computer vision + ML
mediapipe==0.10.14
opencv-python-headless>=4.9.0
deepface>=0.0.89
numpy>=1.24.0,<2.0
Pillow>=10.0.0

# PyTorch — installed as CPU-only in Dockerfile to avoid CUDA overhead
# torch and torchvision are pre-installed before this file runs

# Local LLM inference (optional — falls back to rule-based if not running)
ollama>=0.3.0

# Local vector database (pinned below 1.0 to avoid breaking API changes)
chromadb>=0.5.0,<1.0.0

# Clustering
scikit-learn>=1.4.0

# Object detection (YOLO-World model auto-downloads on first run)
ultralytics>=8.2.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
jinja2>=3.1.3

# Training utilities
tqdm>=4.66.0

# DeepFace backend (tensorflow-cpu avoids 2 GB CUDA build)
tensorflow-cpu>=2.16.0,<2.19.0

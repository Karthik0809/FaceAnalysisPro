services:
  - type: web
    name: face-analysis-pro
    env: docker
    plan: standard        # 2 GB RAM — required for TensorFlow + DeepFace
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: OLLAMA_MODEL
        value: llama3.2
